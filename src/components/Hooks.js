import React, { Component } from 'react'

class Hooks extends Component {
    render() {
        return (
            <div>
                hook component
            </div>
        )
    }
}

export default Hooks
